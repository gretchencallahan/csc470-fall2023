using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CowScript : MonoBehaviour
{
    //public PlaneController planeInfo;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if(planeInfo.beCow == true){
            
            //Vector3 newCamPos = transform.position + -transform.forward * 100 + Vector3.up * 30;
            //cameraObject.transform.position = newCamPos;
            //cameraObject.transform.LookAt(transform);
            
            //if(Input.GetKeyDown(KeyCode.UpArrow)){
                //transform.position += transform.forward * 10;
            //}
            //if(Input.GetKeyDown(KeyCode.DownArrow)){
                //transform.position += transform.forward * -10;
            //}
            //if(Input.GetKeyDown(KeyCode.LeftArrow)){
                //transform.position += Vector3.left * 10;
            //}
            //if(Input.GetKeyDown(KeyCode.UpArrow)){
                //transform.position += Vector3.right * 10;
            //}
        //}
    }
}
